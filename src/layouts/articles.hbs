<!DOCTYPE html>
<html lang="en">
  <head>
    {{>head }}
  </head>
  <body>
    {{>navigation }}
    <div>
    {{{contents}}}
    {{#each articles }}
      <a href="/{{path}}" title="{{description}}">
        <h3>{{ title }}</h3>
      </a>
      <p>{{ description }}</p>
    {{/each }}
    </div>
  </body>
</html>
